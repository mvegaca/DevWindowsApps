<local:MediumSizeListLayout
    x:Class="TheRollingStones.Layouts.ListLayoutBigVerticalCardBox"
    x:Name="root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TheRollingStones.Layouts"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:was_controls="using:AppStudio.Common.Controls"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid x:Name="main">
        <was_controls:ResponsiveGridView
            x:Name="responsiveGridView"
            ItemsSource="{Binding ItemsSource, ElementName=root}"
            ItemClickCommand="{Binding ItemClickCommand, ElementName=root}"
            ItemHeight="{Binding ItemHeight, ElementName=root}">
            <was_controls:ResponsiveGridView.ItemTemplate>
                <DataTemplate>
                    <Grid Height="{Binding ItemHeight, ElementName=root}">
                        <RelativePanel Margin="{Binding ItemMargin, ElementName=root}" Background="{StaticResource BoxBackground}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <Grid x:Name="image" RelativePanel.AlignTopWithPanel="True" Height="{Binding ImageHeight, ElementName=root}">
                                <Image Source="{Binding Image}" Stretch="UniformToFill" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Grid>
                            <RelativePanel RelativePanel.Below="image">
                                <TextBlock x:Name="title" MaxLines="{Binding TitleMaxLines, ElementName=root}" Text="{Binding Title}" Style="{StaticResource BoxTitleStyle}" FontSize="{Binding TitleFontSize, ElementName=root}" RelativePanel.AlignTopWithPanel="True" Margin="{Binding BoxLeftMarginTitle, ElementName=root}"/>
                                <TextBlock Text="{Binding SubTitle}" Style="{StaticResource BoxSubtitleStyle}" RelativePanel.Below="title" FontSize="{Binding SubTitleFontSize, ElementName=root}" Margin="{Binding BoxLeftMarginSubTitle, ElementName=root}"/>
                            </RelativePanel>
                        </RelativePanel>
                    </Grid>
                </DataTemplate>
            </was_controls:ResponsiveGridView.ItemTemplate>
        </was_controls:ResponsiveGridView>
    </Grid>
</local:MediumSizeListLayout>
